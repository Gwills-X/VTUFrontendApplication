const MtnLogo = ({ className }) => (
  <div className={`bg-yellow-400 rounded-full p-4 ${className}`}>
    <span className='font-bold text-black text-lg'>MTN</span>
  </div>
);

export default MtnLogo;
