const GloLogo = ({ className }) => (
  <div className={`bg-green-600 rounded-full p-4 ${className}`}>
    <span className='font-bold text-white text-lg'>Glo</span>
  </div>
);

export default GloLogo;
