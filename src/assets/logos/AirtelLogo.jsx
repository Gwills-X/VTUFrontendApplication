const AirtelLogo = ({ className }) => (
  <div className={`bg-red-500 rounded-full p-4 ${className}`}>
    <span className='font-bold text-white text-lg'>Airtel</span>
  </div>
);

export default AirtelLogo;
